import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF677365);
  static const Color secondary = Color(0xFF50594F);
  static const Color accent = Color(0xFF96A694);
  static const Color light = Color(0xFFB0BFAE);
  static const Color dark = Color(0xFF262620);
  
  static const Color background = dark;
  static const Color surface = primary;
  static const Color textPrimary = light;
  static const Color textSecondary = accent;
}
